language: generic
sudo: required
services:
   - docker

before_install:
   - docker build -t MattJonesKSR/docker-react -f Dockerfile.dev .

script:
   - docker run -e CI=true MattJonesKSR/docker-react npm run test
